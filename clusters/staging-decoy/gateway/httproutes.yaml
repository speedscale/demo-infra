apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: banking-frontend
  namespace: banking-app
spec:
  parentRefs:
  - name: apex-gateway
    namespace: istio-system
  hostnames:
  - apex-banking-staging.trafficreplay.com
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /
    backendRefs:
    - name: banking-frontend
      port: 80
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: grafana
  namespace: observability
spec:
  parentRefs:
  - name: apex-gateway
    namespace: istio-system
  hostnames:
  - apex-grafana-staging.trafficreplay.com
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /
    backendRefs:
    - name: grafana
      port: 3000
---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: jaeger
  namespace: observability
spec:
  parentRefs:
  - name: apex-gateway
    namespace: istio-system
  hostnames:
  - apex-jaeger-staging.trafficreplay.com
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /
    backendRefs:
    - name: jaeger
      port: 16686
# Replay Lab public snapshot webhook endpoints. Keep the dashboard and API private.
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: replay-lab
  namespace: replay-lab
spec:
  parentRefs:
  - name: apex-gateway
    namespace: istio-system
  hostnames:
  - replay-lab-staging.trafficreplay.com
  rules:
  - matches:
    - path:
        type: Exact
        value: /healthz
    - path:
        type: Exact
        value: /readyz
    - path:
        type: PathPrefix
        value: /webhooks
    backendRefs:
    - name: replay-lab-control
      port: 80
