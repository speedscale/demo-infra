apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: byoc-azureblob-staging-decoy
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/speedscale/speedscale-byoc
    targetRevision: main
    path: charts/azureblob
    helm:
      values: |
        azure:
          url: https://speedscalebyoc.blob.core.windows.net/
          container: byoc
          connectionStringSecret: byoc-azureblob
  destination:
    server: https://kubernetes.default.svc
    namespace: byoc-azureblob
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
